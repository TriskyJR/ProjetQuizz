{% extends 'base.html.twig' %}

{% form_theme formQuestion 'bootstrap_4_layout.html.twig' %}

{% block body %}
{% if editMode %}
<h1>Modification de la question</h1>
{% else %}
<h1>Création d'une question !</h1>
{% endif %}
{{ form_start(formQuestion) }}


<div class="form-group">
    {{ form_row(formQuestion.queTitle) }}
</div>

<div class="form-group">
    {{ form_row(formQuestion.queType) }}
</div>

<button type="submit" class="btn btn-success mb-3">Enregistrer</button>

{{ form_end(formQuestion) }}


{% endblock %}
