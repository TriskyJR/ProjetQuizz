{% extends 'base.html.twig' %}

{% block title %}Hello QuizController!{% endblock %}

{% block body %}
    <h1>Questionnaire C#</h1>
    {% for key, question in questions %}
    <div>
        <h1>Question: {{ nbrQuestion[key] }}</h1>
        <p>{{ question.queTitle }}</p>
    </div>
    {% endfor %}
{% endblock %}
